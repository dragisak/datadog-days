kamon {

  metric.tick-interval = 10 seconds

  datadog {
    agent {
      packetbuffer = "tst.DebugBuffer"
    }
    additional-tags {
      service = "no"
      host = "no"
      instance = "no"
    }
  }
}